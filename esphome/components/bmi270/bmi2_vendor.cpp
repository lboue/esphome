
// Intentionally empty.
// The core BMI2 vendor C source (bmi2.c) is compiled via the C wrapper
// (bmi2_vendor.c). Keep this C++ placeholder to avoid compiling the C
// implementation as C++ accidentally.
